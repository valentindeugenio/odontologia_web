---
import { clinic } from '../data/clinic.js';
---

<section class="experience">
  {clinic.experienceHighlights.map((item) => (
    <div class="experience-card"><h3>{item.value}</h3><p>{item.label}</p></div>
  ))}
</section>
